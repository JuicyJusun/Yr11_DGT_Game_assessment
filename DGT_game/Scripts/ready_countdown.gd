extends Node

@onready var ready_countdown = $ready_countdown
@onready var countdown_label = $countdown_label

#func _ready():
#	var time_left = ready_countdown.time_left 
#	ready_countdown.start()
#	countdown_label.set_text(time_left)
